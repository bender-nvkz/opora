{
  "name": "@opora/api-client",
  "version": "0.0.1",
  "private": false,
  "description": "Опора API-клиент — автогенерируемый из OpenAPI",
  "license": "AGPL-3.0-only",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "generate": "openapi-ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@hey-api/openapi-ts": "^0.97.2",
    "typescript": "5.7.2"
  }
}
